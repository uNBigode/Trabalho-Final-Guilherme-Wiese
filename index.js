// Seleciona o botão pelo ID
var botao = document.getElementById('a drum');

// Adiciona um evento de clique ao botão
botao.addEventListener('click', function() {
    alert('Botão clicado no VSCode!');